import chai from 'chai';
import mocha from 'mocha';
import sinon from 'sinon';

const { describe, it } = mocha;
const { expect } = chai;
import { save } from './../src/repository.js';

describe('Repository Suit test', () => {

  it('should save the content on file', () => {

  });

});
